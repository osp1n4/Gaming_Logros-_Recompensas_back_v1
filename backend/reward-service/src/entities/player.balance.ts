// Player Balance Entity
// Represents player's current balance of rewards
// Fields:
// - id (UUID primary key)
// - player_id (UUID foreign key unique)
// - total_coins (integer)
// - total_points (integer)
// - last_updated (timestamp)
