import { AppModule } from './app.module';

/**
 * AppModule smoke test to include module in coverage
 */
describe('AppModule', () => {
  it('should be defined', () => {
    expect(AppModule).toBeDefined();
  });
});
