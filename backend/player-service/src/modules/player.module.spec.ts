import { PlayerModule } from './player.module';

/**
 * PlayerModule smoke test to include module in coverage
 */
describe('PlayerModule', () => {
  it('should be defined', () => {
    expect(PlayerModule).toBeDefined();
  });
});
