// Player Achievement Entity (Pivot Table)
// Represents the relationship between players and achievements
// Fields:
// - id (UUID primary key)
// - player_id (UUID foreign key)
// - achievement_id (UUID foreign key)
// - unlocked_at (timestamp)
// - progress (integer)
